import React from "react";
import { StatsTiles } from "../components/landing/StatsTiles";

export default function LandingPage1() {
  return (
    <main className="bg-white w-full min-h-screen flex items-center justify-center">
      <StatsTiles />
    </main>
  );
}
